---
schema_version: 2
id: token/color-overlay-coral-button
type: token
title: Color — Coral Overlay (Button variant)
status: drifting
last_audited: 2026-05-22
source_doc:
  - doc/colors
  - doc/art-style
  - doc/buttons
---

## Value

`rgba(255, 160, 130, 0.35)` (per `md/COLORS.md` button-variant table; matches what `ButtonBase.js` renders).

**Disputed.** `md/ART_STYLE.md` gives `rgba(255, 111, 97, 0.3)`. See [[drift/colors-accent-overlay-divergence]].

## Usage

Overlay tint for `ButtonBase` variant `coral` — warm accent buttons.

## Notes

Same shape as `token/color-overlay-green-button`. Button context confirms `COLORS.md` is canonical.
