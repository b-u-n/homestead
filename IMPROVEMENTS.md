- food-diary: wrap-up was originally its own step (4) but was folded into the retro step. If a future "week-at-a-glance" view ships, restore the wrap-up step.
- food-diary: owner asked (2026-06) to wrap step 4 into step 5 retro and to drop a "coming soon" label on step 4. R2 mandates the two-step `save`/`saved` shape, so the wrap can't be done in JSON — needs either a renderer change (collapse `save` content into `saved`'s retrospective card) or a relaxation of R2 for diary-tracker activities. Also, no "coming soon" string is present in the JSON — it must be coming from a renderer chrome layer; track that down before claiming the change is applied.
- food-diary: step 3 "outside your usual rhythm" toggle — placement value already maps to the inline row in `BinaryStateToggle.js`, but owner reports the star is rendering above the text. Confirm whether `isInlineRow` is being entered for this entry; if so, the wrap is happening inside `inlineRow` styles when the label is long. Consider switching `inlineLabel` to `flex: 1` so multi-line labels stay to the right of the glyph instead of pushing it up.
